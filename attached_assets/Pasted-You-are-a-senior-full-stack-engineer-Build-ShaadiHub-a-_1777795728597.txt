You are a senior full-stack engineer. Build ShaadiHub — a complete multi-vendor wedding marketplace for Karachi, Pakistan — optimized for Replit. Generate EVERY file completely with NO placeholders.

================================================================
ARCHITECTURE (REPLIT OPTIMIZED)
================================================================

Single repo. FastAPI backend serves React frontend as static files.
Everything runs on ONE port (8000). No CORS issues. No separate servers.

Structure:
shaadihub/
  backend/
    main.py
    database.py
    .env
    requirements.txt
    models/
      __init__.py  user.py  vendor.py  review.py  booking.py  favorite.py  subscription.py
    schemas/
      __init__.py  user.py  vendor.py  review.py
    routers/
      __init__.py  auth.py  vendors.py  users.py  admin.py  bookings.py
    auth/
      __init__.py  jwt.py  dependencies.py
    utils/
      __init__.py  cloudinary_helper.py
  frontend/
    package.json  vite.config.js  tailwind.config.js  postcss.config.js  index.html
    src/
      main.jsx  App.jsx  index.css
      context/AuthContext.jsx
      services/api.js
      layouts/MainLayout.jsx  ProtectedRoute.jsx
      components/common/
        Navbar.jsx  Footer.jsx  VendorCard.jsx  LoadingSpinner.jsx  StarRating.jsx
      pages/
        Home.jsx  VendorListing.jsx  VendorProfile.jsx
        Login.jsx  Register.jsx
        UserDashboard.jsx  VendorDashboard.jsx  AdminPanel.jsx
  .replit
  replit.nix

================================================================
REPLIT CONFIG FILES (CREATE FIRST)
================================================================

FILE: .replit
---
run = "bash start.sh"
[nix]
channel = "stable-23_11"

FILE: start.sh
---
#!/bin/bash
echo "=== Installing Python deps ==="
cd backend && pip install -r requirements.txt -q

echo "=== Building React frontend ==="
cd ../frontend && npm install --silent && npm run build

echo "=== Starting ShaadiHub ==="
cd ../backend && uvicorn main:app --host 0.0.0.0 --port 8000

FILE: replit.nix
---
{ pkgs }: {
  deps = [
    pkgs.python311
    pkgs.python311Packages.pip
    pkgs.nodejs_20
    pkgs.nodePackages.npm
  ];
}

================================================================
DATABASE — SQLite (NO POSTGRESQL NEEDED ON REPLIT)
================================================================

FILE: backend/database.py
---
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
import os

DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./shaadihub.db")

connect_args = {"check_same_thread": False} if "sqlite" in DATABASE_URL else {}
engine = create_engine(DATABASE_URL, connect_args=connect_args)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

FILE: backend/.env
---
DATABASE_URL=sqlite:///./shaadihub.db
SECRET_KEY=shaadihub-super-secret-key-replit-2025-change-this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

NOTE: Put Cloudinary keys in Replit Secrets tab, not in .env file

================================================================
MODELS — ALL 7 FILES
================================================================

FILE: backend/models/user.py
from sqlalchemy import Column, Integer, String, Boolean, DateTime, Enum as SAEnum
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from database import Base
import enum

class UserRole(str, enum.Enum):
    user = "user"
    vendor = "vendor"
    admin = "admin"

class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(100), nullable=False)
    email = Column(String(200), unique=True, index=True, nullable=False)
    password_hash = Column(String(255), nullable=False)
    role = Column(SAEnum(UserRole), default=UserRole.user, nullable=False)
    phone = Column(String(20), nullable=True)
    avatar_url = Column(String(500), nullable=True)
    is_active = Column(Boolean, default=True)
    is_blocked = Column(Boolean, default=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    vendor = relationship("Vendor", back_populates="user", uselist=False)
    reviews = relationship("Review", back_populates="user")
    bookings = relationship("Booking", back_populates="user")
    favorites = relationship("Favorite", back_populates="user")

FILE: backend/models/vendor.py
from sqlalchemy import Column, Integer, String, Float, Boolean, DateTime, ForeignKey, Text, Enum as SAEnum
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from database import Base
import enum

class VendorCategory(str, enum.Enum):
    marriage_hall = "Marriage Hall"
    catering = "Catering"
    photography = "Photography"
    videography = "Videography"
    decoration = "Decoration"
    car_rental = "Car Rental"
    event_planner = "Event Planner"
    music = "Music & DJ"
    makeup = "Makeup & Beauty"
    flowers = "Flowers & Florist"

class Vendor(Base):
    __tablename__ = "vendors"
    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    category = Column(String(50), nullable=False, default="Marriage Hall")
    title = Column(String(200), nullable=False)
    description = Column(Text, nullable=True)
    location = Column(String(300), nullable=True)
    area = Column(String(100), nullable=True)
    city = Column(String(100), default="Karachi")
    latitude = Column(Float, nullable=True)
    longitude = Column(Float, nullable=True)
    price_range_min = Column(Float, nullable=True)
    price_range_max = Column(Float, nullable=True)
    capacity = Column(Integer, nullable=True)
    whatsapp = Column(String(20), nullable=True)
    cover_image_url = Column(String(500), nullable=True)
    verified = Column(Boolean, default=False)
    is_featured = Column(Boolean, default=False)
    is_suspended = Column(Boolean, default=False)
    rating = Column(Float, default=0.0)
    total_reviews = Column(Integer, default=0)
    profile_views = Column(Integer, default=0)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    user = relationship("User", back_populates="vendor")
    images = relationship("VendorImage", back_populates="vendor", cascade="all, delete-orphan")
    packages = relationship("Package", back_populates="vendor", cascade="all, delete-orphan")
    reviews = relationship("Review", back_populates="vendor", cascade="all, delete-orphan")
    bookings = relationship("Booking", back_populates="vendor", cascade="all, delete-orphan")
    favorites = relationship("Favorite", back_populates="vendor", cascade="all, delete-orphan")
    subscription = relationship("Subscription", back_populates="vendor", uselist=False)

class VendorImage(Base):
    __tablename__ = "vendor_images"
    id = Column(Integer, primary_key=True, index=True)
    vendor_id = Column(Integer, ForeignKey("vendors.id"), nullable=False)
    image_url = Column(String(500), nullable=False)
    public_id = Column(String(200), nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    vendor = relationship("Vendor", back_populates="images")

class Package(Base):
    __tablename__ = "packages"
    id = Column(Integer, primary_key=True, index=True)
    vendor_id = Column(Integer, ForeignKey("vendors.id"), nullable=False)
    name = Column(String(100), nullable=False)
    description = Column(Text, nullable=True)
    price = Column(Float, nullable=False)
    features = Column(Text, nullable=True)
    is_popular = Column(Boolean, default=False)
    vendor = relationship("Vendor", back_populates="packages")

FILE: backend/models/review.py
from sqlalchemy import Column, Integer, Float, DateTime, ForeignKey, Text
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from database import Base

class Review(Base):
    __tablename__ = "reviews"
    id = Column(Integer, primary_key=True, index=True)
    vendor_id = Column(Integer, ForeignKey("vendors.id"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    rating = Column(Float, nullable=False)
    comment = Column(Text, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    vendor = relationship("Vendor", back_populates="reviews")
    user = relationship("User", back_populates="reviews")

FILE: backend/models/booking.py
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Text, Enum as SAEnum
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from database import Base
import enum

class BookingStatus(str, enum.Enum):
    pending = "pending"
    approved = "approved"
    rejected = "rejected"
    cancelled = "cancelled"

class Booking(Base):
    __tablename__ = "bookings"
    id = Column(Integer, primary_key=True, index=True)
    vendor_id = Column(Integer, ForeignKey("vendors.id"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    package_id = Column(Integer, ForeignKey("packages.id"), nullable=True)
    event_date = Column(DateTime, nullable=False)
    guest_count = Column(Integer, nullable=True)
    notes = Column(Text, nullable=True)
    status = Column(SAEnum(BookingStatus), default=BookingStatus.pending)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    vendor = relationship("Vendor", back_populates="bookings")
    user = relationship("User", back_populates="bookings")

FILE: backend/models/favorite.py
from sqlalchemy import Column, Integer, DateTime, ForeignKey, UniqueConstraint
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from database import Base

class Favorite(Base):
    __tablename__ = "favorites"
    __table_args__ = (UniqueConstraint("user_id", "vendor_id", name="uq_user_vendor"),)
    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    vendor_id = Column(Integer, ForeignKey("vendors.id"), nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    user = relationship("User", back_populates="favorites")
    vendor = relationship("Vendor", back_populates="favorites")

FILE: backend/models/subscription.py
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Enum as SAEnum
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from database import Base
import enum

class PlanType(str, enum.Enum):
    free = "free"
    premium = "premium"

class Subscription(Base):
    __tablename__ = "subscriptions"
    id = Column(Integer, primary_key=True, index=True)
    vendor_id = Column(Integer, ForeignKey("vendors.id"), nullable=False, unique=True)
    plan = Column(SAEnum(PlanType), default=PlanType.free)
    start_date = Column(DateTime(timezone=True), server_default=func.now())
    end_date = Column(DateTime, nullable=True)
    vendor = relationship("Vendor", back_populates="subscription")

FILE: backend/models/__init__.py
from .user import User, UserRole
from .vendor import Vendor, VendorImage, Package, VendorCategory
from .review import Review
from .booking import Booking, BookingStatus
from .favorite import Favorite
from .subscription import Subscription, PlanType

================================================================
AUTH MODULE — JWT + BCRYPT FIXED
================================================================

FILE: backend/auth/jwt.py
from datetime import datetime, timedelta
from typing import Optional
from jose import JWTError, jwt
from passlib.context import CryptContext
import os

SECRET_KEY = os.getenv("SECRET_KEY", "shaadihub-fallback-secret-key-2025")
ALGORITHM = os.getenv("ALGORITHM", "HS256")
ACCESS_TOKEN_EXPIRE_MINUTES = int(os.getenv("ACCESS_TOKEN_EXPIRE_MINUTES", "1440"))

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def create_access_token(data: dict, expires_delta: Optional[timedelta] = None) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES))
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def decode_token(token: str) -> Optional[dict]:
    try:
        return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    except JWTError:
        return None

FILE: backend/auth/dependencies.py
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.orm import Session
from database import get_db
from models import User
from auth.jwt import decode_token

security = HTTPBearer(auto_error=False)

def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: Session = Depends(get_db)
) -> User:
    if not credentials:
        raise HTTPException(status_code=401, detail="Not authenticated")
    payload = decode_token(credentials.credentials)
    if not payload:
        raise HTTPException(status_code=401, detail="Invalid or expired token")
    user_id = payload.get("sub")
    if not user_id:
        raise HTTPException(status_code=401, detail="Invalid token")
    user = db.query(User).filter(User.id == int(user_id), User.is_active == True).first()
    if not user:
        raise HTTPException(status_code=401, detail="User not found")
    if user.is_blocked:
        raise HTTPException(status_code=403, detail="Account blocked by admin")
    return user

def require_vendor(current_user: User = Depends(get_current_user)) -> User:
    if current_user.role not in ["vendor", "admin"]:
        raise HTTPException(status_code=403, detail="Vendor access required")
    return current_user

def require_admin(current_user: User = Depends(get_current_user)) -> User:
    if current_user.role != "admin":
        raise HTTPException(status_code=403, detail="Admin access required")
    return current_user

FILE: backend/auth/__init__.py
(empty file)

================================================================
SCHEMAS
================================================================

FILE: backend/schemas/user.py
from pydantic import BaseModel, EmailStr, field_validator
from typing import Optional
from datetime import datetime

class UserCreate(BaseModel):
    name: str
    email: EmailStr
    password: str
    role: str = "user"
    phone: Optional[str] = None

    @field_validator('role')
    @classmethod
    def validate_role(cls, v):
        if v not in ['user', 'vendor', 'admin']:
            raise ValueError('Role must be user, vendor, or admin')
        return v

    @field_validator('password')
    @classmethod
    def validate_password(cls, v):
        if len(v) < 6:
            raise ValueError('Password must be at least 6 characters')
        return v

class UserLogin(BaseModel):
    email: EmailStr
    password: str

class UserUpdate(BaseModel):
    name: Optional[str] = None
    phone: Optional[str] = None
    avatar_url: Optional[str] = None

class UserResponse(BaseModel):
    id: int
    name: str
    email: str
    role: str
    phone: Optional[str] = None
    avatar_url: Optional[str] = None
    is_active: bool
    is_blocked: bool
    created_at: datetime
    model_config = {"from_attributes": True}

class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    user: UserResponse

FILE: backend/schemas/vendor.py
from pydantic import BaseModel
from typing import Optional, List
from datetime import datetime

class PackageCreate(BaseModel):
    name: str
    description: Optional[str] = None
    price: float
    features: Optional[str] = None
    is_popular: bool = False

class PackageResponse(BaseModel):
    id: int
    vendor_id: int
    name: str
    description: Optional[str] = None
    price: float
    features: Optional[str] = None
    is_popular: bool
    model_config = {"from_attributes": True}

class VendorImageResponse(BaseModel):
    id: int
    image_url: str
    public_id: Optional[str] = None
    model_config = {"from_attributes": True}

class VendorCreate(BaseModel):
    category: str = "Marriage Hall"
    title: str
    description: Optional[str] = None
    location: Optional[str] = None
    area: Optional[str] = None
    city: str = "Karachi"
    latitude: Optional[float] = None
    longitude: Optional[float] = None
    price_range_min: Optional[float] = None
    price_range_max: Optional[float] = None
    capacity: Optional[int] = None
    whatsapp: Optional[str] = None

class VendorUpdate(BaseModel):
    title: Optional[str] = None
    description: Optional[str] = None
    location: Optional[str] = None
    area: Optional[str] = None
    city: Optional[str] = None
    latitude: Optional[float] = None
    longitude: Optional[float] = None
    price_range_min: Optional[float] = None
    price_range_max: Optional[float] = None
    capacity: Optional[int] = None
    whatsapp: Optional[str] = None
    cover_image_url: Optional[str] = None

class VendorResponse(BaseModel):
    id: int
    user_id: int
    category: str
    title: str
    description: Optional[str] = None
    location: Optional[str] = None
    area: Optional[str] = None
    city: str
    latitude: Optional[float] = None
    longitude: Optional[float] = None
    price_range_min: Optional[float] = None
    price_range_max: Optional[float] = None
    capacity: Optional[int] = None
    whatsapp: Optional[str] = None
    cover_image_url: Optional[str] = None
    verified: bool
    is_featured: bool
    is_suspended: bool
    rating: float
    total_reviews: int
    profile_views: int
    images: List[VendorImageResponse] = []
    packages: List[PackageResponse] = []
    created_at: datetime
    model_config = {"from_attributes": True}

FILE: backend/schemas/review.py
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime

class ReviewCreate(BaseModel):
    rating: float = Field(..., ge=1, le=5)
    comment: Optional[str] = None

class ReviewResponse(BaseModel):
    id: int
    vendor_id: int
    user_id: int
    rating: float
    comment: Optional[str] = None
    created_at: datetime
    user_name: Optional[str] = None
    user_avatar: Optional[str] = None
    model_config = {"from_attributes": True}

class BookingCreate(BaseModel):
    vendor_id: int
    package_id: Optional[int] = None
    event_date: str
    guest_count: Optional[int] = None
    notes: Optional[str] = None

class BookingResponse(BaseModel):
    id: int
    vendor_id: int
    user_id: int
    package_id: Optional[int] = None
    event_date: datetime
    guest_count: Optional[int] = None
    notes: Optional[str] = None
    status: str
    created_at: datetime
    model_config = {"from_attributes": True}

FILE: backend/schemas/__init__.py
(empty file)

================================================================
CLOUDINARY IMAGE UPLOAD (REAL IMAGES — NOT PLACEHOLDER)
================================================================

FILE: backend/utils/cloudinary_helper.py
import cloudinary
import cloudinary.uploader
import os
import uuid

def configure_cloudinary():
    cloudinary.config(
        cloud_name=os.getenv("CLOUDINARY_CLOUD_NAME", ""),
        api_key=os.getenv("CLOUDINARY_API_KEY", ""),
        api_secret=os.getenv("CLOUDINARY_API_SECRET", ""),
        secure=True
    )

def upload_image(file_bytes: bytes, folder: str = "shaadihub") -> dict:
    """Upload image to Cloudinary. Returns {url, public_id}"""
    has_cloudinary = all([
        os.getenv("CLOUDINARY_CLOUD_NAME"),
        os.getenv("CLOUDINARY_API_KEY"),
        os.getenv("CLOUDINARY_API_SECRET")
    ])
    if not has_cloudinary:
        raise Exception("Cloudinary not configured. Add keys to Replit Secrets.")
    configure_cloudinary()
    result = cloudinary.uploader.upload(
        file_bytes,
        folder=folder,
        resource_type="image",
        quality="auto",
        fetch_format="auto"
    )
    return {
        "url": result["secure_url"],
        "public_id": result["public_id"]
    }

def delete_image(public_id: str):
    """Delete image from Cloudinary"""
    try:
        configure_cloudinary()
        cloudinary.uploader.destroy(public_id)
    except Exception:
        pass

FILE: backend/utils/__init__.py
(empty file)

================================================================
ROUTERS — ALL 5 FILES COMPLETE
================================================================

FILE: backend/routers/auth.py
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from database import get_db
from models import User, Vendor
from schemas.user import UserCreate, UserLogin, UserResponse, TokenResponse
from auth.jwt import hash_password, verify_password, create_access_token

router = APIRouter(prefix="/api/auth", tags=["Authentication"])

@router.post("/register", response_model=TokenResponse)
def register(data: UserCreate, db: Session = Depends(get_db)):
    existing = db.query(User).filter(User.email == data.email.lower()).first()
    if existing:
        raise HTTPException(status_code=400, detail="Email already registered")
    user = User(
        name=data.name.strip(),
        email=data.email.lower(),
        password_hash=hash_password(data.password),
        role=data.role,
        phone=data.phone
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    if data.role == "vendor":
        vendor = Vendor(
            user_id=user.id,
            category="Marriage Hall",
            title=f"{data.name.strip()}'s Business",
            city="Karachi"
        )
        db.add(vendor)
        db.commit()
    token = create_access_token({"sub": str(user.id), "role": user.role})
    return TokenResponse(access_token=token, user=UserResponse.model_validate(user))

@router.post("/login", response_model=TokenResponse)
def login(data: UserLogin, db: Session = Depends(get_db)):
    user = db.query(User).filter(User.email == data.email.lower()).first()
    if not user or not verify_password(data.password, user.password_hash):
        raise HTTPException(status_code=401, detail="Invalid email or password")
    if not user.is_active:
        raise HTTPException(status_code=403, detail="Account deactivated")
    if user.is_blocked:
        raise HTTPException(status_code=403, detail="Account blocked by admin")
    token = create_access_token({"sub": str(user.id), "role": user.role})
    return TokenResponse(access_token=token, user=UserResponse.model_validate(user))

FILE: backend/routers/vendors.py
from fastapi import APIRouter, Depends, HTTPException, Query, UploadFile, File
from sqlalchemy.orm import Session
from sqlalchemy import or_
from typing import Optional
from database import get_db
from models import User, Vendor, VendorImage, Package
from schemas.vendor import VendorCreate, VendorUpdate, VendorResponse, PackageCreate, PackageResponse
from auth.dependencies import get_current_user, require_vendor
from utils.cloudinary_helper import upload_image, delete_image

router = APIRouter(prefix="/api/vendors", tags=["Vendors"])

@router.get("", response_model=dict)
def list_vendors(
    category: Optional[str] = None, area: Optional[str] = None,
    city: Optional[str] = None, min_price: Optional[float] = None,
    max_price: Optional[float] = None, min_rating: Optional[float] = None,
    capacity: Optional[int] = None, search: Optional[str] = None,
    featured: Optional[bool] = None, page: int = Query(1, ge=1),
    limit: int = Query(12, le=50), db: Session = Depends(get_db)
):
    q = db.query(Vendor).filter(Vendor.is_suspended == False)
    if category: q = q.filter(Vendor.category == category)
    if area: q = q.filter(Vendor.area.ilike(f"%{area}%"))
    if city: q = q.filter(Vendor.city.ilike(f"%{city}%"))
    if min_price is not None: q = q.filter(Vendor.price_range_min >= min_price)
    if max_price is not None: q = q.filter(Vendor.price_range_max <= max_price)
    if min_rating is not None: q = q.filter(Vendor.rating >= min_rating)
    if capacity is not None: q = q.filter(Vendor.capacity >= capacity)
    if featured is not None: q = q.filter(Vendor.is_featured == featured)
    if search: q = q.filter(or_(
        Vendor.title.ilike(f"%{search}%"),
        Vendor.description.ilike(f"%{search}%"),
        Vendor.location.ilike(f"%{search}%")
    ))
    total = q.count()
    vendors = q.order_by(Vendor.is_featured.desc(), Vendor.rating.desc()).offset((page-1)*limit).limit(limit).all()
    return {"total": total, "page": page, "pages": max(1, (total+limit-1)//limit), "vendors": [VendorResponse.model_validate(v) for v in vendors]}

@router.get("/me/profile", response_model=VendorResponse)
def get_my_profile(current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.user_id == current_user.id).first()
    if not vendor:
        raise HTTPException(status_code=404, detail="Vendor profile not found")
    return vendor

@router.get("/{vendor_id}", response_model=VendorResponse)
def get_vendor(vendor_id: int, db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.id == vendor_id, Vendor.is_suspended == False).first()
    if not vendor: raise HTTPException(status_code=404, detail="Vendor not found")
    vendor.profile_views += 1
    db.commit()
    db.refresh(vendor)
    return vendor

@router.post("", response_model=VendorResponse)
def create_vendor(data: VendorCreate, current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    existing = db.query(Vendor).filter(Vendor.user_id == current_user.id).first()
    if existing: raise HTTPException(status_code=400, detail="Vendor profile already exists")
    vendor = Vendor(user_id=current_user.id, **data.model_dump())
    db.add(vendor)
    db.commit()
    db.refresh(vendor)
    return vendor

@router.put("/{vendor_id}", response_model=VendorResponse)
def update_vendor(vendor_id: int, data: VendorUpdate, current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Not found")
    if vendor.user_id != current_user.id and current_user.role != "admin":
        raise HTTPException(status_code=403, detail="Not authorized")
    for k, v in data.model_dump(exclude_none=True).items():
        setattr(vendor, k, v)
    db.commit()
    db.refresh(vendor)
    return vendor

@router.post("/{vendor_id}/images")
async def upload_vendor_image(vendor_id: int, file: UploadFile = File(...), current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Not found")
    if vendor.user_id != current_user.id and current_user.role != "admin":
        raise HTTPException(status_code=403, detail="Not authorized")
    if not file.content_type.startswith("image/"):
        raise HTTPException(status_code=400, detail="Only image files allowed")
    contents = await file.read()
    if len(contents) > 10 * 1024 * 1024:
        raise HTTPException(status_code=400, detail="Image too large. Max 10MB.")
    try:
        result = upload_image(contents, folder="shaadihub/vendors")
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Upload failed: {str(e)}")
    img = VendorImage(vendor_id=vendor_id, image_url=result["url"], public_id=result.get("public_id"))
    db.add(img)
    if not vendor.cover_image_url:
        vendor.cover_image_url = result["url"]
    db.commit()
    db.refresh(img)
    return {"id": img.id, "image_url": result["url"], "public_id": result.get("public_id")}

@router.delete("/{vendor_id}/images/{image_id}")
def delete_vendor_image(vendor_id: int, image_id: int, current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    img = db.query(VendorImage).filter(VendorImage.id == image_id, VendorImage.vendor_id == vendor_id).first()
    if not img: raise HTTPException(status_code=404, detail="Image not found")
    if img.public_id:
        delete_image(img.public_id)
    db.delete(img)
    db.commit()
    return {"message": "Deleted"}

@router.post("/{vendor_id}/packages", response_model=PackageResponse)
def add_package(vendor_id: int, data: PackageCreate, current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.id == vendor_id, Vendor.user_id == current_user.id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Not found")
    pkg = Package(vendor_id=vendor_id, **data.model_dump())
    db.add(pkg)
    db.commit()
    db.refresh(pkg)
    return pkg

@router.put("/{vendor_id}/packages/{pkg_id}", response_model=PackageResponse)
def update_package(vendor_id: int, pkg_id: int, data: PackageCreate, current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    pkg = db.query(Package).filter(Package.id == pkg_id, Package.vendor_id == vendor_id).first()
    if not pkg: raise HTTPException(status_code=404, detail="Not found")
    for k, v in data.model_dump().items():
        setattr(pkg, k, v)
    db.commit()
    db.refresh(pkg)
    return pkg

@router.delete("/{vendor_id}/packages/{pkg_id}")
def delete_package(vendor_id: int, pkg_id: int, current_user: User = Depends(require_vendor), db: Session = Depends(get_db)):
    pkg = db.query(Package).filter(Package.id == pkg_id, Package.vendor_id == vendor_id).first()
    if not pkg: raise HTTPException(status_code=404, detail="Not found")
    db.delete(pkg)
    db.commit()
    return {"message": "Deleted"}

FILE: backend/routers/users.py
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from typing import List
from datetime import datetime
from database import get_db
from models import User, Vendor, Favorite, Booking
from models.review import Review
from schemas.user import UserUpdate, UserResponse
from schemas.review import ReviewCreate, ReviewResponse, BookingCreate, BookingResponse
from schemas.vendor import VendorResponse
from auth.dependencies import get_current_user

router = APIRouter(prefix="/api/users", tags=["Users"])

@router.get("/me", response_model=UserResponse)
def get_me(current_user: User = Depends(get_current_user)):
    return current_user

@router.put("/me", response_model=UserResponse)
def update_me(data: UserUpdate, current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    for k, v in data.model_dump(exclude_none=True).items():
        setattr(current_user, k, v)
    db.commit()
    db.refresh(current_user)
    return current_user

@router.post("/favorites/{vendor_id}")
def toggle_favorite(vendor_id: int, current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Vendor not found")
    existing = db.query(Favorite).filter(Favorite.user_id == current_user.id, Favorite.vendor_id == vendor_id).first()
    if existing:
        db.delete(existing)
        db.commit()
        return {"favorited": False, "message": "Removed from favorites"}
    fav = Favorite(user_id=current_user.id, vendor_id=vendor_id)
    db.add(fav)
    db.commit()
    return {"favorited": True, "message": "Added to favorites"}

@router.get("/favorites", response_model=List[VendorResponse])
def get_favorites(current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    favs = db.query(Favorite).filter(Favorite.user_id == current_user.id).all()
    if not favs: return []
    ids = [f.vendor_id for f in favs]
    return db.query(Vendor).filter(Vendor.id.in_(ids)).all()

@router.post("/bookings", response_model=BookingResponse)
def create_booking(data: BookingCreate, current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.id == data.vendor_id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Vendor not found")
    try:
        event_dt = datetime.fromisoformat(data.event_date.replace("Z", "+00:00"))
    except ValueError:
        raise HTTPException(status_code=400, detail="Invalid date format. Use ISO format: 2025-12-25")
    booking = Booking(
        vendor_id=data.vendor_id, user_id=current_user.id,
        package_id=data.package_id, event_date=event_dt,
        guest_count=data.guest_count, notes=data.notes
    )
    db.add(booking)
    db.commit()
    db.refresh(booking)
    return booking

@router.get("/bookings", response_model=List[BookingResponse])
def get_my_bookings(current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    return db.query(Booking).filter(Booking.user_id == current_user.id).order_by(Booking.created_at.desc()).all()

@router.get("/reviews/{vendor_id}", response_model=List[ReviewResponse])
def get_reviews(vendor_id: int, db: Session = Depends(get_db)):
    reviews = db.query(Review).filter(Review.vendor_id == vendor_id).order_by(Review.created_at.desc()).all()
    result = []
    for r in reviews:
        resp = ReviewResponse.model_validate(r)
        if r.user:
            resp.user_name = r.user.name
            resp.user_avatar = r.user.avatar_url
        result.append(resp)
    return result

@router.post("/reviews/{vendor_id}", response_model=ReviewResponse)
def add_review(vendor_id: int, data: ReviewCreate, current_user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    if current_user.role == "vendor":
        raise HTTPException(status_code=403, detail="Vendors cannot review other vendors")
    existing = db.query(Review).filter(Review.user_id == current_user.id, Review.vendor_id == vendor_id).first()
    if existing: raise HTTPException(status_code=400, detail="You already reviewed this vendor")
    vendor = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Vendor not found")
    review = Review(vendor_id=vendor_id, user_id=current_user.id, rating=data.rating, comment=data.comment)
    db.add(review)
    db.flush()
    all_reviews = db.query(Review).filter(Review.vendor_id == vendor_id).all()
    vendor.rating = round(sum(r.rating for r in all_reviews) / len(all_reviews), 1)
    vendor.total_reviews = len(all_reviews)
    db.commit()
    db.refresh(review)
    resp = ReviewResponse.model_validate(review)
    resp.user_name = current_user.name
    resp.user_avatar = current_user.avatar_url
    return resp

FILE: backend/routers/bookings.py
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from typing import List
from database import get_db
from models import Vendor, Booking
from schemas.review import BookingResponse
from auth.dependencies import require_vendor, get_current_user

router = APIRouter(prefix="/api/bookings", tags=["Bookings"])

VALID_STATUSES = ["approved", "rejected", "cancelled"]

@router.get("/vendor", response_model=List[BookingResponse])
def get_vendor_bookings(current_user=Depends(require_vendor), db: Session = Depends(get_db)):
    vendor = db.query(Vendor).filter(Vendor.user_id == current_user.id).first()
    if not vendor: raise HTTPException(status_code=404, detail="Vendor profile not found")
    return db.query(Booking).filter(Booking.vendor_id == vendor.id).order_by(Booking.created_at.desc()).all()

@router.put("/{booking_id}/status")
def update_status(booking_id: int, status: str, current_user=Depends(require_vendor), db: Session = Depends(get_db)):
    if status not in VALID_STATUSES:
        raise HTTPException(status_code=400, detail=f"Status must be one of: {VALID_STATUSES}")
    booking = db.query(Booking).filter(Booking.id == booking_id).first()
    if not booking: raise HTTPException(status_code=404, detail="Booking not found")
    vendor = db.query(Vendor).filter(Vendor.user_id == current_user.id).first()
    if vendor and booking.vendor_id != vendor.id and current_user.role != "admin":
        raise HTTPException(status_code=403, detail="Not authorized")
    booking.status = status
    db.commit()
    return {"message": f"Status updated to {status}", "booking_id": booking_id, "status": status}

FILE: backend/routers/admin.py
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from typing import List
from database import get_db
from models import User, Vendor, Booking
from models.review import Review
from schemas.vendor import VendorResponse
from schemas.user import UserResponse
from auth.dependencies import require_admin

router = APIRouter(prefix="/api/admin", tags=["Admin"])

@router.get("/stats")
def get_stats(db: Session = Depends(get_db), _=Depends(require_admin)):
    return {
        "total_users": db.query(User).filter(User.role == "user").count(),
        "total_vendors": db.query(Vendor).count(),
        "verified_vendors": db.query(Vendor).filter(Vendor.verified == True).count(),
        "pending_vendors": db.query(Vendor).filter(Vendor.verified == False, Vendor.is_suspended == False).count(),
        "featured_vendors": db.query(Vendor).filter(Vendor.is_featured == True).count(),
        "total_bookings": db.query(Booking).count(),
        "total_reviews": db.query(Review).count(),
        "suspended_vendors": db.query(Vendor).filter(Vendor.is_suspended == True).count(),
    }

@router.get("/vendors", response_model=List[VendorResponse])
def list_vendors(db: Session = Depends(get_db), _=Depends(require_admin)):
    return db.query(Vendor).order_by(Vendor.created_at.desc()).all()

@router.put("/vendors/{vendor_id}/approve")
def approve_vendor(vendor_id: int, db: Session = Depends(get_db), _=Depends(require_admin)):
    v = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not v: raise HTTPException(status_code=404, detail="Not found")
    v.verified = True
    db.commit()
    return {"message": "Vendor approved", "vendor_id": vendor_id}

@router.put("/vendors/{vendor_id}/suspend")
def suspend_vendor(vendor_id: int, db: Session = Depends(get_db), _=Depends(require_admin)):
    v = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not v: raise HTTPException(status_code=404, detail="Not found")
    v.is_suspended = not v.is_suspended
    db.commit()
    return {"message": "Toggled", "is_suspended": v.is_suspended}

@router.put("/vendors/{vendor_id}/feature")
def feature_vendor(vendor_id: int, db: Session = Depends(get_db), _=Depends(require_admin)):
    v = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not v: raise HTTPException(status_code=404, detail="Not found")
    v.is_featured = not v.is_featured
    db.commit()
    return {"message": "Toggled", "is_featured": v.is_featured}

@router.delete("/vendors/{vendor_id}")
def delete_vendor(vendor_id: int, db: Session = Depends(get_db), _=Depends(require_admin)):
    v = db.query(Vendor).filter(Vendor.id == vendor_id).first()
    if not v: raise HTTPException(status_code=404, detail="Not found")
    db.delete(v)
    db.commit()
    return {"message": "Deleted"}

@router.get("/users", response_model=List[UserResponse])
def list_users(db: Session = Depends(get_db), _=Depends(require_admin)):
    return db.query(User).order_by(User.created_at.desc()).all()

@router.put("/users/{user_id}/block")
def block_user(user_id: int, db: Session = Depends(get_db), _=Depends(require_admin)):
    u = db.query(User).filter(User.id == user_id).first()
    if not u: raise HTTPException(status_code=404, detail="Not found")
    if u.role == "admin": raise HTTPException(status_code=400, detail="Cannot block admin")
    u.is_blocked = not u.is_blocked
    db.commit()
    return {"message": "Toggled", "is_blocked": u.is_blocked}

FILE: backend/routers/__init__.py
(empty file)

================================================================
MAIN.PY — SERVES REACT + API BOTH
================================================================

FILE: backend/main.py
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse
from fastapi.middleware.cors import CORSMiddleware
from dotenv import load_dotenv
import os, pathlib

load_dotenv()

from database import engine, Base
import models

from routers import auth, vendors, users, admin, bookings

Base.metadata.create_all(bind=engine)

app = FastAPI(title="ShaadiHub API", version="1.0.0", docs_url="/api/docs", redoc_url="/api/redoc")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(auth.router)
app.include_router(vendors.router)
app.include_router(users.router)
app.include_router(admin.router)
app.include_router(bookings.router)

@app.get("/api/health")
def health():
    return {"status": "healthy", "service": "ShaadiHub API"}

FRONTEND_DIST = pathlib.Path(__file__).parent.parent / "frontend" / "dist"

if FRONTEND_DIST.exists():
    app.mount("/assets", StaticFiles(directory=str(FRONTEND_DIST / "assets")), name="assets")

    @app.get("/{full_path:path}")
    def serve_react(full_path: str):
        index = FRONTEND_DIST / "index.html"
        return FileResponse(str(index))
else:
    @app.get("/")
    def root():
        return {"message": "ShaadiHub API running. Build React frontend first.", "docs": "/api/docs"}

================================================================
REQUIREMENTS.TXT
================================================================

FILE: backend/requirements.txt
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.3
pydantic-settings==2.3.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
cloudinary==1.40.0
python-dotenv==1.0.1
email-validator==2.1.1
aiosqlite==0.20.0

================================================================
FRONTEND — REACT + VITE + TAILWIND
================================================================

FILE: frontend/vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
export default defineConfig({
  plugins: [react()],
  server: { port: 5173 },
  build: { outDir: 'dist', emptyOutDir: true }
})

FILE: frontend/package.json
{
  "name": "shaadihub-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "lucide-react": "^0.395.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.24.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "vite": "^5.3.1"
  }
}

FILE: frontend/tailwind.config.js
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  theme: { extend: {} },
  plugins: [],
}

FILE: frontend/postcss.config.js
export default { plugins: { tailwindcss: {}, autoprefixer: {} } }

FILE: frontend/index.html



  
  
  


  

  



================================================================
FRONTEND API SERVICE — FIXED FOR SAME ORIGIN
================================================================

FILE: frontend/src/services/api.js
import axios from 'axios';

const BASE = import.meta.env.VITE_API_URL || '';

const api = axios.create({ baseURL: BASE });

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('shaadihub_token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  (err) => {
    if (err.response?.status === 401) {
      const url = err.config?.url || '';
      if (!url.includes('/auth/login') && !url.includes('/auth/register')) {
        localStorage.removeItem('shaadihub_token');
        localStorage.removeItem('shaadihub_user');
        window.location.href = '/login';
      }
    }
    return Promise.reject(err);
  }
);

export const authAPI = {
  register: (d) => api.post('/api/auth/register', d),
  login: (d) => api.post('/api/auth/login', d),
};

export const vendorAPI = {
  list: (p) => api.get('/api/vendors', { params: p }),
  get: (id) => api.get(`/api/vendors/${id}`),
  getMyProfile: () => api.get('/api/vendors/me/profile'),
  create: (d) => api.post('/api/vendors', d),
  update: (id, d) => api.put(`/api/vendors/${id}`, d),
  uploadImage: (id, fd) => api.post(`/api/vendors/${id}/images`, fd, { headers: { 'Content-Type': 'multipart/form-data' } }),
  deleteImage: (id, imgId) => api.delete(`/api/vendors/${id}/images/${imgId}`),
  addPackage: (id, d) => api.post(`/api/vendors/${id}/packages`, d),
  updatePackage: (id, pkgId, d) => api.put(`/api/vendors/${id}/packages/${pkgId}`, d),
  deletePackage: (id, pkgId) => api.delete(`/api/vendors/${id}/packages/${pkgId}`),
};

export const userAPI = {
  getMe: () => api.get('/api/users/me'),
  updateMe: (d) => api.put('/api/users/me', d),
  getFavorites: () => api.get('/api/users/favorites'),
  toggleFavorite: (id) => api.post(`/api/users/favorites/${id}`),
  getBookings: () => api.get('/api/users/bookings'),
  createBooking: (d) => api.post('/api/users/bookings', d),
  getReviews: (id) => api.get(`/api/users/reviews/${id}`),
  addReview: (id, d) => api.post(`/api/users/reviews/${id}`, d),
};

export const bookingAPI = {
  getVendorBookings: () => api.get('/api/bookings/vendor'),
  updateStatus: (id, status) => api.put(`/api/bookings/${id}/status?status=${status}`),
};

export const adminAPI = {
  getStats: () => api.get('/api/admin/stats'),
  listVendors: () => api.get('/api/admin/vendors'),
  approveVendor: (id) => api.put(`/api/admin/vendors/${id}/approve`),
  suspendVendor: (id) => api.put(`/api/admin/vendors/${id}/suspend`),
  featureVendor: (id) => api.put(`/api/admin/vendors/${id}/feature`),
  deleteVendor: (id) => api.delete(`/api/admin/vendors/${id}`),
  listUsers: () => api.get('/api/admin/users'),
  blockUser: (id) => api.put(`/api/admin/users/${id}/block`),
};

export default api;

================================================================
AUTH CONTEXT — FIXED
================================================================

FILE: frontend/src/context/AuthContext.jsx
import { createContext, useContext, useState } from 'react';
import { authAPI } from '../services/api';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(() => {
    try {
      const s = localStorage.getItem('shaadihub_user');
      return s ? JSON.parse(s) : null;
    } catch { return null; }
  });

  const login = async (email, password) => {
    const res = await authAPI.login({ email, password });
    const { access_token, user: u } = res.data;
    localStorage.setItem('shaadihub_token', access_token);
    localStorage.setItem('shaadihub_user', JSON.stringify(u));
    setUser(u);
    return u;
  };

  const register = async (data) => {
    const res = await authAPI.register(data);
    const { access_token, user: u } = res.data;
    localStorage.setItem('shaadihub_token', access_token);
    localStorage.setItem('shaadihub_user', JSON.stringify(u));
    setUser(u);
    return u;
  };

  const logout = () => {
    localStorage.removeItem('shaadihub_token');
    localStorage.removeItem('shaadihub_user');
    setUser(null);
  };

  const updateUser = (updates) => {
    const updated = { ...user, ...updates };
    localStorage.setItem('shaadihub_user', JSON.stringify(updated));
    setUser(updated);
  };

  return (
    
      {children}
    
  );
}

export const useAuth = () => {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error('useAuth must be used within AuthProvider');
  return ctx;
};

================================================================
APP.JSX — ROUTES FIXED
================================================================

FILE: frontend/src/App.jsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { AuthProvider, useAuth } from './context/AuthContext';
import MainLayout from './layouts/MainLayout';
import Home from './pages/Home';
import VendorListing from './pages/VendorListing';
import VendorProfile from './pages/VendorProfile';
import Login from './pages/Login';
import Register from './pages/Register';
import UserDashboard from './pages/UserDashboard';
import VendorDashboard from './pages/VendorDashboard';
import AdminPanel from './pages/AdminPanel';

function ProtectedRoute({ children, roles }) {
  const { user } = useAuth();
  if (!user) return ;
  if (roles && !roles.includes(user.role)) {
    if (user.role === 'admin') return ;
    if (user.role === 'vendor') return ;
    return ;
  }
  return children;
}

export default function App() {
  return (
    
      
        
          }>
            } />
            } />
            } />
            } />
            } />
            
                
              
            } />
            
                
              
            } />
            
                
              
            } />
            } />
          
        
      
    
  );
}

================================================================
FRONTEND PAGES — ALL 8 COMPLETE
================================================================

Generate ALL 8 pages with FULL working code:

1. HOME PAGE (src/pages/Home.jsx):
- Navbar with logo (heart SVG icon + ShaadiHub text)
- Hero section: big heading, search bar that navigates to /vendors?search=query
- Stats: 2500+ couples, 400+ vendors, 20+ areas in Karachi, 4.8/5 rating
- Category grid (8 categories with emoji + labels, each links to /vendors?category=...)
  Categories: Marriage Hall🏛️ Catering🍽️ Photography📸 Car Rental🚗 Event Planner📋 Decoration🌸 Music & DJ🎵 Makeup & Beauty💄
- Featured Vendors: call GET /api/vendors?featured=true&limit=6, show VendorCard grid
  If no featured vendors found, show regular vendors (call GET /api/vendors?limit=6 as fallback)
- Why ShaadiHub: 3 benefit cards
- CTA banner: rose gradient, Get Started Free + Browse Vendors buttons
- Footer

2. VENDOR LISTING (src/pages/VendorListing.jsx):
- Read ?search and ?category from URL params on mount
- Sticky top search bar
- Collapsible sidebar filters: category (8 options), area (Clifton, DHA, Gulshan, North Nazimabad, Saddar, Korangi, Malir, Bahria Town, Scheme 33, PECHS), price min/max, rating min
- Vendor grid: 3 cols desktop, 2 tablet, 1 mobile using VendorCard
- Pagination controls

3. VENDOR PROFILE (src/pages/VendorProfile.jsx):
- Full image gallery: show cover_image_url first, then vendor.images array. Show arrows if >1 image
- IMPORTANT: Use actual  tags for real Cloudinary images. NOT placeholder divs.
- Show category badge, Verified badge (shield icon), Featured badge (star icon)
- Rating display, location, capacity
- Description text
- Packages section: 3-column grid (1 col mobile). Most popular card highlighted in rose-500 border
  Each package: name, price in PKR formatted with toLocaleString(), description, feature list
  Feature list: parse JSON string from pkg.features — try JSON.parse, if error show as plain text
  "Book This Package" button → sets package_id in booking form + opens modal
- Location section: if vendor.latitude && vendor.longitude, show Google Maps iframe
  src={`https://maps.google.com/maps?q=${lat},${lng}&z=15&output=embed`}
- Reviews section: call GET /api/users/reviews/{id}, show list with user initial avatar, name, stars, comment, date
- "Write a Review" button (only show if user.role === 'user') → opens review modal
- RIGHT SIDEBAR (sticky):
  - "Starting from PKR X" price
  - "Book Now" button → opens booking modal
  - "WhatsApp Inquiry" button (only if vendor.whatsapp exists)
    href={`https://wa.me/92${vendor.whatsapp.replace(/\D/g,'')}`} target="_blank"
  - Trust badges: Verified, Secure Booking, Free Cancellation
  
BOOKING MODAL (inside VendorProfile):
- Date input (type="date", min=today)
- Guest count number input
- Notes textarea
- On submit: call POST /api/users/bookings with {vendor_id, package_id, event_date, guest_count, notes}
- Show success message on complete, error message on failure
- Only accessible if user is logged in. If not logged in show "Please login to book"

REVIEW MODAL (inside VendorProfile):
- Clickable star rating (1-5)
- Comment textarea
- Submit button
- Prevent if user already reviewed (show error from API)

4. LOGIN (src/pages/Login.jsx):
- Clean centered card design
- Email + password fields
- Show/hide password toggle
- Error message display
- On submit: call authAPI.login, save token + user to localStorage, redirect based on role:
  - admin → /admin
  - vendor → /vendor/dashboard
  - user → /dashboard
- Link to /register

5. REGISTER (src/pages/Register.jsx):
- Role toggle buttons at top: "Couple / Guest" (role=user) and "Wedding Vendor" (role=vendor)
- Name, email, phone (optional), password fields
- Password min 6 chars validation
- On submit: call authAPI.register, redirect based on role
- Link to /login

6. USER DASHBOARD (src/pages/UserDashboard.jsx):
- Sidebar tabs: Overview, Favorites, Bookings, Checklist
- OVERVIEW TAB:
  - 3 stat cards: Saved Vendors (favorites.length), Bookings (bookings.length), Tasks Done
  - Recent bookings list (last 3): booking ID, event date, status badge
- FAVORITES TAB:
  - Grid of favorited vendors, each with cover image, title, category, price
  - Remove from favorites button (calls toggleFavorite → removes if already favorited)
- BOOKINGS TAB:
  - List all bookings: event date, guest count, notes, status
  - Status badge colors: pending=amber, approved=green, rejected=red, cancelled=gray
- CHECKLIST TAB:
  - 10 wedding tasks with checkboxes: "Set wedding date", "Book venue", "Book caterer", "Book photographer", "Book decorator", "Send invitations", "Book car rental", "Arrange accommodation", "Order wedding cake", "Plan honeymoon"
  - Save checked state to localStorage key "shaadihub_checklist"
  - Progress bar showing X/10 completed

7. VENDOR DASHBOARD (src/pages/VendorDashboard.jsx):
- On mount: call GET /api/vendors/me/profile and GET /api/bookings/vendor
- Sidebar tabs: Overview, Profile, Portfolio, Packages, Bookings, Reviews
- OVERVIEW TAB:
  - 4 stat cards: Profile Views, Bookings, Reviews, Rating
  - Recent bookings with approve/reject buttons (for status=pending only)
  - Upgrade to Premium card (rose gradient)
- PROFILE TAB:
  - Display all vendor fields (title, description, location, area, whatsapp, prices, capacity, lat, lng)
  - "Edit Profile" button → switches inputs to editable mode
  - "Save" button → calls PUT /api/vendors/{id} → shows success
  - "Cancel" button → reverts to display mode
- PORTFOLIO TAB:
  - Show vendor.images as actual  grid (NOT placeholders)
  - Each image has X delete button → calls DELETE /api/vendors/{id}/images/{imageId}
  - Upload Photo button (file input, accept="image/*") → creates FormData → calls POST /api/vendors/{id}/images → reloads profile
  - Show upload progress/loading state
  - If no Cloudinary configured: show clear message "Configure Cloudinary in Replit Secrets to enable image upload"
- PACKAGES TAB:
  - List all packages with name, price, description
  - Edit and Delete buttons per package
  - "Add Package" button → opens modal
  - PACKAGE MODAL fields: name, price (number), description, features (textarea, one per line = stored as JSON array), is_popular checkbox
  - Save → calls POST or PUT as appropriate
- BOOKINGS TAB:
  - Table: Booking ID, Event Date, Guests, Notes, Status, Actions
  - Approve/Reject buttons only for pending bookings
- REVIEWS TAB:
  - List reviews: avatar initial, reviewer name, star rating, comment, date
  - Average rating summary at top

8. ADMIN PANEL (src/pages/AdminPanel.jsx):
- Sidebar tabs: Statistics, Vendors, Users
- STATS TAB: 8 metric cards from GET /api/admin/stats
- VENDORS TAB: scrollable table
  - Columns: Business Name, Category, City, Rating, Status badges (Verified/Featured/Suspended/Pending), Actions
  - Actions: Approve (green check — only if !verified), Feature ⭐ (toggle), Suspend 🚫 (toggle), Delete 🗑️
  - Confirm before delete
- USERS TAB: table
  - Columns: Name, Email, Role badge, Status (Active/Blocked), Joined date, Actions
  - Block/Unblock button per user
  - Do NOT show block button for admin users

================================================================
COMPONENTS
================================================================

FILE: src/components/common/Navbar.jsx
- Logo: heart SVG icon + "Shaadi" + "Hub" (Hub in rose-500)
- Desktop links: Vendors, Venues, Photographers, Catering
- If logged in: user avatar (initial letter in rose circle) + name + dropdown chevron
  Dropdown: Dashboard link (role-based URL), Logout button
- If not logged in: "Log In" + "Join Free" buttons
- Mobile: hamburger menu

FILE: src/components/common/VendorCard.jsx
- Cover image: if cover_image_url exists, use  
  If NO image: show gray div with category emoji (use a map: Marriage Hall→🏛️, Catering→🍽️, etc.)
- Featured badge (rose pill), Verified badge (emerald pill) — top-left of image
- Heart button top-right (only functional if user logged in, else redirect to /login)
  Call toggleFavorite, update local state
- Category pill bottom of image (dark bg)
- Below image: title, area/city, rating stars + count, capacity (if set)
- Price: "From PKR X" or "PKR X – PKR Y" formatted with toLocaleString()
- Entire card is a Link to /vendors/{id}

FILE: src/components/common/StarRating.jsx
- Props: rating (number), onChange (function, optional), size ('sm'|'md'|'lg')
- If onChange provided: clickable stars
- If not: display only
- Yellow fill for rated stars, gray for unrated

FILE: src/components/common/LoadingSpinner.jsx
- Centered spinning circle,